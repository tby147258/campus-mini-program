# 校园综合服务平台 - 开发规范

## 0. 项目上下文（重要：Claude Code优先读取此文件）

### 0.1 项目目录结构
```
c:\Users\24361\Desktop\实训\
├── .clinerules              # 本文件——开发规范（Claude Code自动加载）
├── .claudeignore            # Claude Code忽略规则（排除docx/pdf大文件）
├── .gitignore               # Git忽略规则
├── README.md                # 项目总览
├── campus-backend/          # SpringBoot后端
│   └── src/main/java/com/campus/
│       ├── CampusApplication.java
│       ├── common/          # Result, JwtUtil, BusinessException, MetaObjectHandler
│       ├── config/          # JwtConfig, MybatisPlusConfig, WebMvcConfig
│       ├── controller/      # AuthController, AnnouncementController, LostFoundController, RepairOrderController, FileController, WeatherController
│       ├── entity/          # User, Announcement, LostFound, RepairOrder, RepairLog, SystemConfig
│       ├── mapper/          # UserMapper, AnnouncementMapper, LostFoundMapper, RepairOrderMapper, SystemConfigMapper
│       └── service/         # UserService, AnnouncementService, LostFoundService, RepairOrderService
├── campus-admin/            # Vue3管理后台
│   └── src/
│       ├── api/index.js     # 统一API封装
│       ├── router/index.js  # 路由配置
│       └── views/           # Login, Layout, Dashboard, AnnouncementList, AnnouncementForm, LostFoundManage, RepairManage, UserManage, Statistics, Settings
├── campus-miniapp/          # 微信小程序
│   └── pages/               # index(首页), lostfound, repair, profile
└── docs/database/           # 数据库设计（SQL脚本可读，docx文件被.claudeignore排除）
    ├── schema.sql           # 7张表建表脚本
    ├── test_data.sql        # 测试数据
    ├── data_dictionary.md   # 数据字典
    └── er_diagram.svg       # E-R图
```

### 0.2 数据库核心信息（7张表）
- **user**：12字段 — id, open_id(UK), password(BCrypt), nickname, avatar, student_no, phone, role(0学生/1管理员), status(0正常/1禁用), created_at, updated_at, is_deleted
- **announcement**：10字段 — id, title, content, category, publisher_id(FK→user), view_count, status(0草稿/1已发布), created_at, updated_at, is_deleted
- **lost_found**：17字段 — id, type(0失物/1寻物), item_name, category, images, description, location, contact_person, contact_phone, status(0待审核/1已发布/2未通过/3已结束), user_id(FK), auditor_id(FK), audit_time, reject_reason, created_at, updated_at, is_deleted
- **repair_order**：21字段 — id, order_no(UK,R+日期+序列), repair_type, campus, building, room, description, images, contact_person, contact_phone, status(0待处理/1处理中/2已完成/3已驳回), reject_reason, handle_result, user_id(FK), handler_id(FK), estimated_complete_time, created_at, handle_time, complete_time, updated_at, is_deleted
- **repair_log**：8字段 — id, order_id(FK→repair_order), operator_id(FK→user), action, from_status, to_status, remark, created_at
- **operation_log**：8字段 — id, user_id(FK), module, action, target_id, description, ip_address, created_at
- **system_config**：6字段 — id, config_key(UK), config_value, description, created_at, updated_at

### 0.3 关键依赖版本
- SpringBoot 3.2.0, MyBatis-Plus 3.5.5, MySQL 8.0.39, JJWT 0.12.3
- Vue 3.4, Element Plus 2.5, Pinia 2.1, Vite 5.0, Axios 1.6
- Lombok, spring-security-crypto (BCrypt), springdoc-openapi 2.3

### 0.4 项目配置要点
- 数据库：localhost:3306/campus, user=root, password=root
- JWT密钥：campus-service-jwt-secret-key-2026, 过期时间86400秒(24h)
- 后端端口：8080
- 前端端口：5173（Vite默认）
- 跨域：已配置WebMvcConfig允许所有origin

## 1. 项目概述
- 项目名称：校园综合服务平台
- 架构：前后端分离，RESTful API
- 前端二：Vue3管理后台 + 微信小程序
- 后端：SpringBoot 3.2 + MyBatis-Plus 3.5.5 + MySQL 8.0 + JWT
- 管理后台：Vue 3 + Element Plus + Pinia + Vite
- 小程序：微信小程序原生框架
- 开发工具：VSCode + Claude Code + DeepSeek

## 2. MVC分层架构（后端SpringBoot）

### 2.1 Controller层
- 统一使用@RestController注解
- 遵循RESTful风格，路径使用复数名词
- 统一返回Result<T>封装
- 参数校验使用@Valid注解
- 接口路径格式：/api/{module}/...
- 方法命名：list/page/get/create/update/delete/audit

### 2.2 Service层
- 接口+实现类分离（IUserService + UserServiceImpl）
- 事务注解：@Transactional(rollbackFor = Exception.class)
- 业务逻辑写在Service层，Controller不写业务逻辑
- 方法命名：getById/save/update/remove/page/list

### 2.3 Mapper层
- 继承MyBatis-Plus的BaseMapper
- 复杂查询使用@Select或XML
- 分页使用MyBatis-Plus的Page对象

### 2.4 Entity层
- 统一使用@TableName注解
- 主键策略：@TableId(type = IdType.AUTO)
- 逻辑删除：@TableLogic
- 审计字段：created_at, updated_at（自动填充）
- 统一使用is_deleted逻辑删除

## 3. 数据库规范
- 表名：小写字母+下划线
- 字段名：小写字母+下划线
- 主键统一命名为id
- 外键以关联表名_id结尾
- 所有表含created_at, updated_at
- 核心业务表含is_deleted
- 所有字段加COMMENT注释
- 字符集：utf8mb4，排序规则：utf8mb4_general_ci
- 引擎：InnoDB

## 4. 后端代码规范
- 包结构：com.campus.{controller/service/mapper/entity/config/common}
- 统一异常处理：全局@RestControllerAdvice
- 跨域配置：统一WebMvcConfig
- JWT认证：JwtUtil工具类，拦截器统一校验
- 密码加密：BCrypt
- 日志：使用Slf4j

## 5. Vue3管理后台规范
- 状态管理：Pinia
- UI组件库：Element Plus
- 路由：Vue Router，懒加载
- API调用统一封装在src/api/目录
- 页面组件放在src/views/目录
- 命名：多单词驼峰，如UserManage.vue

## 6. 微信小程序规范
- 4个Tab页：首页/失物招领/报修中心/个人中心
- 页面放在pages/目录下
- WXML使用flex布局
- 请求封装统一方法
- 使用wx.request调用后端API

## 7. API设计规范
- 统一前缀：/api/
- 认证接口：/api/auth/**
- 公告接口：/api/announcements/**
- 失物招领：/api/lost-found/**
- 报修工单：/api/repair-orders/**
- 文件上传：/api/files/**
- 天气查询：/api/weather/**
- 分页参数：page, size
- 响应格式：{code, message, data}

## 8. 通用约定
- 代码必须有中文注释
- 实体类字段加@ApiModelProperty或注释
- 敏感信息（密码等）不返回前端
- 文件上传限制大小和类型
- 使用逻辑删除，不使用物理删除